import 'package:flutter/material.dart';
class DetailsCity extends StatelessWidget {
  const DetailsCity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
